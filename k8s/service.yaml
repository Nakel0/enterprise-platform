apiVersion: v1
kind: Service
metadata:
  name: preview-app-service
  labels:
    app: preview-app 
spec:
  type: LoadBalancer
  selector:
    app: preview-app
  ports:
    - port: 80
      targetPort: 3000
      name: http